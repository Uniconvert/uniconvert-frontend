# 아직 연결하지 않은 도메인은 Mock API를 사용합니다.
VITE_USE_MOCK_API=true

# true로 바꾸면 전체 Mock 모드에서도 인증만 실제 서버에 연결합니다.
VITE_USE_REAL_AUTH_API=false

# 실제 API에는 /api/v1 접두사가 없습니다.
VITE_API_BASE_URL=https://api.uniconvert.dev
